# LIBERO benchmark runtime dependencies.
#
# Installed only when setup.sh is invoked with --with-benchmark. Torch,
# torchvision, robomimic, transformers, wandb, and thop are intentionally
# excluded so benchmark setup cannot replace the existing inference runtime.

robosuite==1.4.0
bddl>=3.6.0
gym==0.26.2
imageio>=2.30,<3
imageio-ffmpeg>=0.4.9
hydra-core==1.2.0
easydict>=1.9,<2
einops>=0.4.1,<0.9
cloudpickle>=2.1.0,<4
matplotlib>=3.5.0,<4
numba==0.66.0
llvmlite==0.48.0
